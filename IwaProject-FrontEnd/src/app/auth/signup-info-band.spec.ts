import { SignupInfoBand } from './signup-info-band';

describe('SignupInfoBand', () => {
  it('should create an instance', () => {
    // @ts-ignore
    expect(new SignupInfoBand()).toBeTruthy();
  });
});

import { SignupInfoOrg } from './signup-info-org';

describe('SignupInfoOrg', () => {
  it('should create an instance', () => {
    // @ts-ignore
    expect(new SignupInfoOrg()).toBeTruthy();
  });
});
